// constants/motion.js
// All animation timing constants — single source of truth

export const CURSOR_BLINK_MS   = 800;   // cursor block blink period
export const TYPEWRITER_SPEED  = 28;    // ms per character (normal responses)
export const TYPEWRITER_BOOT   = 22;    // ms per character (boot sequence, slightly faster)
export const THINKING_DOT_MS   = 400;   // stagger per thinking dot
export const THINKING_DELAY_MS = 500;   // how long "thinking..." shows before typing starts
export const MASCOT_BOUNCE_MS  = 300;   // mascot bounce animation duration
